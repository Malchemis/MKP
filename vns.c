//
// Variable Neighborhood Search.
// Uses VND and a perturbation method.
//

#include "lib/vns.h"
#include <stdio.h>
#include <stdlib.h>

void vns(const Problem *prob, Solution *sol, void (*eval_func)(const Problem*, Solution*)) {
    // Placeholder:
    // A real VNS would:
    // 1. Apply VND until no improvement.
    // 2. Perturb solution.
    // 3. Evaluate and repeat if better solutions are found.
    (void)prob; (void)sol; (void)eval_func;
}

