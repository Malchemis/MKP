//
// Variable Neighborhood Descent.
// Uses 2 local search algorithms.
//

#include "lib/vnd.h"

void vnd(const Problem *prob, Solution *sol, void (*eval_func)(const Problem*, Solution*)) {
    // Placeholder: In a real VND, you'd call two different local_search variants,
    // switching neighborhoods when no improvement is found.
    (void)prob; (void)sol; (void)eval_func;
}

